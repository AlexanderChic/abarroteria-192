{
  "name": "abarroteria-api",
  "version": "1.0.0",
  "description": "JSON Server para Abarrotería Jardines del Edén",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch src/data/db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "18.x"
  }
}