{
  "name": "abarroteria-api",
  "version": "1.0.0",
  "description": "API para sistema de abarrotería",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch data/db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}